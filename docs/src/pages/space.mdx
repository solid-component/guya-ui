# Space 间距

# 何时使用
避免组件紧贴在一起，拉开统一的空间。

- 适合行内元素的水平间距。
- 可以设置各种水平对齐方式。

# 基础使用

<ComponentPreview name="space" demo="basic"/>

# 间隙大小
使用 size 设置元素之间的间距，预设了 small、middle、large 三种尺寸，
<ComponentPreview name="space" demo="size"/>

